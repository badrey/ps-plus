import {StyleSheet} from "react-native";
import {ScreenPadding} from "../settings";

export const commonStyles = StyleSheet.create({
    flexOne: {
        flex: 1,
    },
    paddingHorizontal: {
        paddingHorizontal: ScreenPadding,
    },
    padding: {
        padding: ScreenPadding,
    },
});
